# Model profile matching the official baseline checkpoint.
#
# published_total_parameters is the number the 32B challenge budget counts:
# the official *published* total, not active parameters for an MoE model, and
# unaffected by quantisation.
#
# AUDIT STATUS: UNVERIFIED. The value below is transcribed from the checkpoint
# name in benchmark/configs/baseline-qwen-3.5-9b.yaml, which the architecture
# spec (section 2.2) explicitly says is not sufficient evidence. Confirm the
# figure against the official model card and record the URL in `source:`
# before this profile is used for any reported result.

model_profile:
  backend: huggingface
  model_id: "Qwen/Qwen3.5-9B"
  published_total_parameters: 9_000_000_000
  family: qwen
  revision: main
  role: generator
  license: ""
  source: ""
  notes: >-
    Parameter count inferred from the checkpoint name and NOT yet verified
    against the published model card. Verify before reporting results.
